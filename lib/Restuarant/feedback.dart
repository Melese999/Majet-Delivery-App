import 'package:flutter/material.dart';

class res_feedback extends StatefulWidget {
  const res_feedback({Key? key}) : super(key: key);

  @override
  State<res_feedback> createState() => _res_feedback();
}

class _res_feedback extends State<res_feedback> {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
