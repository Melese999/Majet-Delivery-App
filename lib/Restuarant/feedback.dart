import 'package:flutter/material.dart';

class res_feedback extends StatefulWidget {
  const res_feedback({Key? key}) : super(key: key);

  @override
  State<res_feedback> createState() => _menuState();
}

class _menuState extends State<res_feedback> {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
